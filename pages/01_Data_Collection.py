import streamlit as st

st.title("📥 데이터 수집 과정")

st.markdown("""
본 프로젝트에서는 **기후 변화**와 관련된 사회적 이슈를 분석하기 위해  
네이버 뉴스 검색 API를 활용하여 뉴스 기사 데이터를 수집하였다.

실시간 API 호출은 배포 환경의 안정성을 고려하여  
분석 단계에서는 CSV 파일 형태로 저장된 데이터를 사용하였다.
""")

st.divider()

st.subheader("🔍 데이터 수집 개요")

st.markdown("""
- **수집 키워드**: 기후 변화  
- **데이터 출처**: 네이버 뉴스 검색 API  
- **수집 데이터 수**: 100건  
- **저장 형식**: CSV 파일  
""")

st.divider()

st.subheader("🧩 사용한 API 호출 코드 예시")

st.markdown("""
아래는 뉴스 데이터를 수집할 때 사용한  
네이버 뉴스 검색 API 호출 코드의 핵심 부분이다.
""")

st.code("""
url = "https://openapi.naver.com/v1/search/news?query=기후%20변화&display=100"

request = urllib.request.Request(url)
request.add_header("X-Naver-Client-Id", client_id)
request.add_header("X-Naver-Client-Secret", client_secret)

response = urllib.request.urlopen(request)
""", language="python")

st.divider()

st.subheader("📁 데이터 저장")

st.markdown("""
수집된 뉴스 데이터는 분석 및 재사용을 위해  
CSV 파일로 저장하였다.

이를 통해 API 호출 횟수를 줄이고,  
안정적인 웹 애플리케이션 배포가 가능하도록 구성하였다.
""")